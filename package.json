{
  "name": "rotina-produtiva-michel",
  "version": "0.1.0",
  "private": true,
  "description": "Rotina Produtiva do Michel + placar de tráfego (Katzer OS).",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "postinstall": "node scripts/netlify-postinstall-funil.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
