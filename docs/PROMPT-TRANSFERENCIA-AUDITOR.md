# Prompt de transferência — do Auditor para o Tráfego

Cole o texto abaixo na sessão/IA do **repo do Auditor** (Katzer). Ele devolve os
arquivos que o repo do Tráfego precisa. **Só arquivo entra** — os repos ficam
separados (doutrina). Depois é só me mandar a saída (upload ou colar aqui) que eu
ponho no lugar certo e substituo os placeholders.

---

```
Você está no repo do AUDITOR (Katzer). Preciso EXPORTAR dois arquivos para o repo
separado de TRÁFEGO PAGO. Não altere nada no Auditor — só me entregue o conteúdo.

Faça, na ordem:

1. Localize e me devolva o CONTEÚDO BRUTO, COMPLETO e sem cortes de:
   a) docs/prototipos/quadradinho-decisao.html
   b) docs/FUNCAO-MICHEL-IA.md
   Entregue cada um em um bloco de código separado, com o caminho original no
   topo. Não resuma, não reescreva, não "melhore" — cópia fiel.

2. Se algum dos caminhos não existir exatamente assim, procure pelo nome
   (quadradinho-decisao / FUNCAO-MICHEL-IA) e me diga o caminho real antes de
   colar. Se não achar, diga "NÃO ENCONTRADO" — não invente conteúdo.

3. NÃO inclua maestro/src/michel.js nem a reguaDoMichel(): isso é do Auditor e
   fica FORA do escopo do Tráfego. Se quiser, descreva em 2 linhas o que a régua
   faz (só pra referência), mas NÃO cole o código.

4. Confirme no fim: "Esses arquivos podem ir pro repo de Tráfego? (s/n)" — porque
   o cruzamento entre repos é por arquivo/contrato, e a autorização é humana.

Saída esperada: os 2 arquivos em blocos de código + a linha de confirmação.
```

---

## Depois que o Auditor devolver

1. Me mande a saída (upload dos arquivos, ou cole aqui).
2. Eu substituo os placeholders:
   - `docs/prototipos/quadradinho-decisao.html`
   - `docs/FUNCAO-MICHEL-IA.md`
3. Commito e empurro. Os placeholders somem, entra o conteúdo real.
